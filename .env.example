MONGODB_URI=mongodb+srv://...
JWT_SECRET=
TMDB_READ_TOKEN=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# After first deploy, run: node scripts/seed-admin.mjs
# This creates the marcos admin user in the DB.
# All other users are managed from the /admin panel.
