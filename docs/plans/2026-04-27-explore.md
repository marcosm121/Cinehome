# Explorar Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Agregar una pantalla `/explore` accesible desde un botón en la home que muestra secciones curadas (Populares, Mejor valoradas) y permite filtrar películas por género, año y rating mínimo.

**Architecture:** Un nuevo API endpoint `/api/movies/discover` envuelve TMDB `/discover/movie` con parámetros opcionales y usa el caché existente (`tmdbGet`). La página es un Client Component con estado local que alterna entre modo curado (sin filtros) y modo filtrado (con grilla + botón "Cargar más"). El acceso es desde un botón en la home, sin cambios en el nav.

**Tech Stack:** Next.js App Router, TypeScript, SWR, MongoDB (caché vía `tmdbGet`), TMDB API, CSS inline con variables CSS existentes.

> **IMPORTANTE antes de escribir código:** Leer `node_modules/next/dist/docs/` si hay dudas sobre APIs de Next.js — puede haber breaking changes respecto al conocimiento de entrenamiento.

---

## Task 1: API endpoint `/api/movies/discover`

**Files:**
- Create: `app/api/movies/discover/route.ts`

**Step 1: Crear el archivo**

```ts
// app/api/movies/discover/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { getSession } from '@/lib/getSession'
import { tmdbGet, normalizeTmdbMovie } from '@/lib/tmdb'

export async function GET(req: NextRequest) {
  const session = await getSession(req)
  if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })

  const { searchParams } = req.nextUrl
  const genres = searchParams.get('genres') ?? ''
  const year = searchParams.get('year') ?? ''
  const minRating = searchParams.get('minRating') ?? ''
  const sort = searchParams.get('sort') ?? 'popularity.desc'
  const page = searchParams.get('page') ?? '1'

  const params = new URLSearchParams({
    language: 'es-MX',
    sort_by: sort,
    page,
    include_adult: 'false',
  })
  if (genres) params.set('with_genres', genres)
  if (year) params.set('primary_release_year', year)
  if (minRating) params.set('vote_average.gte', minRating)
  // Exigir mínimo de votos para evitar películas sin calificación
  if (minRating) params.set('vote_count.gte', '100')

  const cacheKey = `discover:${params.toString()}`

  try {
    const data = await tmdbGet(`/discover/movie?${params.toString()}`, cacheKey) as Record<string, any>
    const results = (data.results ?? []).map(normalizeTmdbMovie)
    return NextResponse.json({
      results,
      page: data.page ?? 1,
      totalPages: Math.min(data.total_pages ?? 1, 20), // TMDB limita a 500 páginas, nosotros a 20
    })
  } catch (err) {
    console.error('[movies/discover]', err)
    return NextResponse.json({ error: 'Error al explorar películas' }, { status: 502 })
  }
}
```

**Step 2: Verificar manualmente**

Con el servidor corriendo (`npm run dev`), abrir en browser:
```
http://localhost:3000/api/movies/discover
http://localhost:3000/api/movies/discover?genres=28&minRating=7&year=2023
http://localhost:3000/api/movies/discover?sort=vote_average.desc&minRating=7
```
Confirmar que devuelve `{ results: [...], page: 1, totalPages: N }` con películas normalizadas.

**Step 3: Commit**

```bash
git add app/api/movies/discover/route.ts
git commit -m "feat: add /api/movies/discover endpoint"
```

---

## Task 2: Explore page — estructura, filtros y género chips

**Files:**
- Create: `app/(app)/explore/page.tsx` (esqueleto inicial)

El GENRE_MAP ya existe en `lib/tmdb.ts` como `Record<number, string>` pero es privado al módulo. Necesitamos una lista exportada para los chips. La agregamos como export en `lib/tmdb.ts`.

**Step 1: Exportar GENRE_MAP desde `lib/tmdb.ts`**

Agregar `export` al `GENRE_MAP` existente en `lib/tmdb.ts` (línea 11):

```ts
// Cambiar:
const GENRE_MAP: Record<number, string> = {
// Por:
export const GENRE_MAP: Record<number, string> = {
```

**Step 2: Crear la página con estado y filtros**

```tsx
// app/(app)/explore/page.tsx
'use client'
import { useState, useCallback } from 'react'
import useSWR from 'swr'
import { MovieShelf } from '@/components/MovieShelf'
import { MovieCard } from '@/components/MovieCard'
import { GENRE_MAP } from '@/lib/tmdb'
import type { NormalizedMovie } from '@/lib/tmdb'

const fetcher = (url: string) => fetch(url).then(r => r.json())

// Años disponibles: desde el actual hasta 1970
const CURRENT_YEAR = new Date().getFullYear()
const YEARS = Array.from({ length: CURRENT_YEAR - 1969 }, (_, i) => CURRENT_YEAR - i)

const RATING_OPTIONS = [
  { label: 'Cualquier rating', value: '' },
  { label: '6+ ★', value: '6' },
  { label: '7+ ★', value: '7' },
  { label: '7.5+ ★', value: '7.5' },
  { label: '8+ ★', value: '8' },
]

const GENRES = Object.entries(GENRE_MAP).map(([id, name]) => ({ id: Number(id), name }))

export default function ExplorePage() {
  const [selectedGenres, setSelectedGenres] = useState<number[]>([])
  const [year, setYear] = useState('')
  const [minRating, setMinRating] = useState('')
  const [results, setResults] = useState<NormalizedMovie[]>([])
  const [page, setPage] = useState(1)
  const [totalPages, setTotalPages] = useState(1)
  const [isLoadingMore, setIsLoadingMore] = useState(false)

  const hasActiveFilters = selectedGenres.length > 0 || year !== '' || minRating !== ''

  // SWR para secciones curadas (solo se usan cuando no hay filtros)
  const popularUrl = '/api/movies/discover?sort=popularity.desc&page=1'
  const topRatedUrl = '/api/movies/discover?sort=vote_average.desc&minRating=7&page=1'
  const { data: popularData } = useSWR(popularUrl, fetcher)
  const { data: topRatedData } = useSWR(topRatedUrl, fetcher)
  const popular: NormalizedMovie[] = popularData?.results ?? []
  const topRated: NormalizedMovie[] = topRatedData?.results ?? []

  // Fetch de resultados filtrados (se dispara manualmente)
  const fetchFiltered = useCallback(async (pageNum: number, append: boolean) => {
    const params = new URLSearchParams({ page: String(pageNum) })
    if (selectedGenres.length) params.set('genres', selectedGenres.join(','))
    if (year) params.set('year', year)
    if (minRating) params.set('minRating', minRating)

    const res = await fetch(`/api/movies/discover?${params.toString()}`)
    const data = await res.json()
    setResults(prev => append ? [...prev, ...(data.results ?? [])] : (data.results ?? []))
    setPage(data.page ?? 1)
    setTotalPages(data.totalPages ?? 1)
  }, [selectedGenres, year, minRating])

  function toggleGenre(id: number) {
    setSelectedGenres(prev =>
      prev.includes(id) ? prev.filter(g => g !== id) : [...prev, id]
    )
  }

  // Resetear y buscar cuando cambian filtros
  function applyFilters() {
    setResults([])
    setPage(1)
    fetchFiltered(1, false)
  }

  async function loadMore() {
    setIsLoadingMore(true)
    await fetchFiltered(page + 1, true)
    setIsLoadingMore(false)
  }

  function clearFilters() {
    setSelectedGenres([])
    setYear('')
    setMinRating('')
    setResults([])
    setPage(1)
  }

  return (
    <div style={{ paddingBottom: 90 }}>
      {/* Header */}
      <div style={{ padding: '20px 22px 0' }}>
        <h1 style={{ fontSize: 26, fontWeight: 800, letterSpacing: '-0.8px', color: 'var(--ink)', margin: '0 0 16px' }}>
          Explorar
        </h1>
      </div>

      {/* Chips de géneros */}
      <div style={{
        overflowX: 'auto', display: 'flex', gap: 8,
        padding: '0 22px 4px', WebkitOverflowScrolling: 'touch' as any,
        scrollbarWidth: 'none' as any,
      }}>
        {GENRES.map(g => {
          const active = selectedGenres.includes(g.id)
          return (
            <button
              key={g.id}
              onClick={() => toggleGenre(g.id)}
              style={{
                flexShrink: 0, padding: '7px 14px',
                borderRadius: 'var(--radius-md)',
                border: active ? 'none' : '1px solid var(--line-strong)',
                background: active ? 'var(--accent)' : 'var(--bg-card)',
                color: active ? '#000' : 'var(--ink-mute)',
                fontSize: 13, fontWeight: active ? 700 : 400,
                cursor: 'pointer', whiteSpace: 'nowrap',
              }}
            >
              {g.name}
            </button>
          )
        })}
      </div>

      {/* Fila de selectores: Año + Rating */}
      <div style={{ display: 'flex', gap: 10, padding: '12px 22px 0' }}>
        <select
          value={year}
          onChange={e => setYear(e.target.value)}
          style={selectStyle}
        >
          <option value="">Cualquier año</option>
          {YEARS.map(y => <option key={y} value={y}>{y}</option>)}
        </select>
        <select
          value={minRating}
          onChange={e => setMinRating(e.target.value)}
          style={selectStyle}
        >
          {RATING_OPTIONS.map(o => <option key={o.value} value={o.value}>{o.label}</option>)}
        </select>
      </div>

      {/* Botón aplicar / limpiar */}
      <div style={{ display: 'flex', gap: 10, padding: '12px 22px 0' }}>
        <button
          onClick={applyFilters}
          style={{
            flex: 1, padding: '10px 0',
            background: 'var(--accent)', border: 'none',
            borderRadius: 'var(--radius-md)',
            color: '#000', fontSize: 14, fontWeight: 700, cursor: 'pointer',
          }}
        >
          Buscar
        </button>
        {hasActiveFilters && (
          <button
            onClick={clearFilters}
            style={{
              padding: '10px 18px',
              background: 'var(--bg-card)', border: '1px solid var(--line-strong)',
              borderRadius: 'var(--radius-md)',
              color: 'var(--ink-mute)', fontSize: 13, cursor: 'pointer',
            }}
          >
            Limpiar
          </button>
        )}
      </div>

      {/* Contenido principal */}
      <div style={{ marginTop: 24 }}>
        {!hasActiveFilters || results.length === 0 && !isLoadingMore ? (
          // Modo curado
          <>
            <Section title="Populares ahora">
              <MovieShelf movies={popular} />
            </Section>
            <Section title="Mejor valoradas">
              <MovieShelf movies={topRated} />
            </Section>
          </>
        ) : (
          // Modo filtrado
          <div style={{ padding: '0 22px' }}>
            <div style={{ fontSize: 12, color: 'var(--ink-mute)', marginBottom: 16 }}>
              Página {page} de {totalPages}
            </div>
            <div style={{
              display: 'grid',
              gridTemplateColumns: 'repeat(2, 1fr)',
              gap: 16,
            }}>
              {results.map(movie => (
                <MovieCard key={movie.tmdbId} movie={movie} variant="grid" />
              ))}
            </div>
            {page < totalPages && (
              <button
                onClick={loadMore}
                disabled={isLoadingMore}
                style={{
                  width: '100%', marginTop: 24,
                  padding: '12px 0',
                  background: 'var(--bg-card)', border: '1px solid var(--line-strong)',
                  borderRadius: 'var(--radius-md)',
                  color: isLoadingMore ? 'var(--ink-mute)' : 'var(--ink)',
                  fontSize: 14, fontWeight: 600, cursor: isLoadingMore ? 'default' : 'pointer',
                }}
              >
                {isLoadingMore ? 'Cargando…' : 'Cargar más'}
              </button>
            )}
          </div>
        )}
      </div>
    </div>
  )
}

const selectStyle: React.CSSProperties = {
  flex: 1, padding: '9px 12px',
  background: 'var(--bg-card)', border: '1px solid var(--line-strong)',
  borderRadius: 'var(--radius-md)',
  color: 'var(--ink)', fontSize: 13, cursor: 'pointer',
  appearance: 'none', WebkitAppearance: 'none',
}

function Section({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <div style={{ marginBottom: 36 }}>
      <h2 style={{
        fontSize: 19, fontWeight: 700, letterSpacing: '-0.4px',
        color: 'var(--ink)', padding: '0 22px', margin: '0 0 14px',
      }}>
        {title}
      </h2>
      {children}
    </div>
  )
}
```

**Step 3: Verificar en browser**

Navegar a `http://localhost:3000/explore`. Confirmar:
- Se ven dos secciones curadas con `MovieShelf`
- Los chips de géneros hacen scroll horizontal en mobile
- Los selects de año y rating funcionan
- El botón "Buscar" y "Limpiar" aparecen correctamente

**Step 4: Commit**

```bash
git add lib/tmdb.ts app/\(app\)/explore/page.tsx
git commit -m "feat: add explore page with filter chips and curated sections"
```

---

## Task 3: Explore page — modo filtrado + "Cargar más"

> Este task no requiere archivos nuevos. La lógica ya está en el código del Task 2. Verificar que funciona correctamente.

**Step 1: Prueba manual del flujo filtrado**

1. En `/explore`, seleccionar el género "Acción"
2. Presionar "Buscar"
3. Confirmar que la grilla de 2 columnas aparece con películas de acción
4. Scrollear hasta abajo, presionar "Cargar más"
5. Confirmar que se agregan más películas debajo sin limpiar las anteriores
6. Probar combinar género + año + rating
7. Presionar "Limpiar" → confirmar que vuelven las secciones curadas

**Step 2: Fix de la condición de renderizado**

La condición actual `!hasActiveFilters || results.length === 0 && !isLoadingMore` puede ser confusa. Reemplazarla por una más explícita en el código del Task 2 si es necesario:

```tsx
// Mostrar curado si: no hay filtros activos, O hay filtros pero no se buscó todavía
const showCurated = !hasActiveFilters || (hasActiveFilters && results.length === 0 && !isLoadingMore)
```

Y usar `showCurated` en el JSX:
```tsx
{showCurated ? (
  // secciones curadas
) : (
  // grilla filtrada
)}
```

---

## Task 4: Loading skeleton

**Files:**
- Create: `app/(app)/explore/loading.tsx`

**Step 1: Crear el archivo**

```tsx
// app/(app)/explore/loading.tsx
import { Skeleton } from '@/components/Skeleton'

export default function ExploreLoading() {
  return (
    <div style={{ paddingBottom: 90 }}>
      <div style={{ padding: '20px 22px 0' }}>
        <Skeleton width={160} height={32} borderRadius={8} />
      </div>
      {/* Chips placeholder */}
      <div style={{ display: 'flex', gap: 8, padding: '16px 22px 4px', overflow: 'hidden' }}>
        {Array.from({ length: 5 }).map((_, i) => (
          <Skeleton key={i} width={80} height={34} borderRadius={8} />
        ))}
      </div>
      {/* Selectores placeholder */}
      <div style={{ display: 'flex', gap: 10, padding: '12px 22px 0' }}>
        <Skeleton width="50%" height={40} borderRadius={8} />
        <Skeleton width="50%" height={40} borderRadius={8} />
      </div>
      {/* Shelf placeholders */}
      <div style={{ marginTop: 32, padding: '0 22px' }}>
        <Skeleton width={160} height={22} borderRadius={6} />
        <div style={{ display: 'flex', gap: 12, marginTop: 14 }}>
          {Array.from({ length: 4 }).map((_, i) => (
            <Skeleton key={i} width={128} height={192} borderRadius={8} />
          ))}
        </div>
      </div>
    </div>
  )
}
```

**Step 2: Verificar**

Navegar a `/explore` con conexión lenta simulada en DevTools. Confirmar que el skeleton aparece antes de las películas.

**Step 3: Commit**

```bash
git add app/\(app\)/explore/loading.tsx
git commit -m "feat: add explore loading skeleton"
```

---

## Task 5: Botón "Explorar" en la home

**Files:**
- Modify: `app/(app)/page.tsx`

**Step 1: Agregar el botón debajo de `TonightCard`**

En `app/(app)/page.tsx`, localizar donde se renderiza `<TonightCard lists={lists} />` y agregar el botón debajo:

```tsx
<TonightCard lists={lists} />
<ExploreButton />
```

Agregar el componente `ExploreButton` en el mismo archivo:

```tsx
function ExploreButton() {
  return (
    <Link
      href="/explore"
      style={{
        display: 'flex', alignItems: 'center', justifyContent: 'space-between',
        margin: '12px 22px 0',
        padding: '14px 16px',
        borderRadius: 'var(--radius-lg)',
        background: 'var(--bg-card)', border: '1px solid var(--line-strong)',
        textDecoration: 'none', color: 'inherit',
      }}
    >
      <div>
        <div style={{ fontSize: 11, color: 'var(--accent)', fontWeight: 600, marginBottom: 4 }}>
          ✦ Descubrir películas
        </div>
        <div style={{ fontSize: 15, fontWeight: 700, color: 'var(--ink)' }}>
          Explorar catálogo
        </div>
      </div>
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="var(--ink-mute)" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
        <circle cx="11" cy="11" r="8"/>
        <line x1="21" y1="21" x2="16.65" y2="16.65"/>
      </svg>
    </Link>
  )
}
```

**Step 2: Verificar en browser**

En la home, confirmar que el botón aparece visualmente debajo del card "Qué ver esta noche", tiene el estilo consistente con la app, y navega correctamente a `/explore` al tocarlo.

**Step 3: Commit**

```bash
git add app/\(app\)/page.tsx
git commit -m "feat: add explore button to home page"
```

---

## Task 6: Verificación final y smoke test

**Step 1: Flujo completo**

1. Home → ver botón "Explorar catálogo"
2. Tocar botón → llegar a `/explore`
3. Ver secciones "Populares ahora" y "Mejor valoradas"
4. Seleccionar un género (ej. "Drama")
5. Presionar "Buscar" → ver grilla
6. Presionar "Cargar más" → más resultados
7. Presionar "Limpiar" → volver a curado
8. Combinar géneros + año + rating → buscar
9. Navegar a una película desde los resultados → detalle correcto
10. Volver a `/explore` con el botón back → estado reseteado (comportamiento correcto: cada visita empieza sin filtros)

**Step 2: Verificar en mobile**

Con DevTools en modo mobile (o en el teléfono):
- Chips de géneros hacen scroll horizontal sin wrap
- Los selects son usables con el teclado nativo
- La grilla es de 2 columnas
- El botón "Cargar más" es full-width
- No hay overflow horizontal en ninguna sección

**Step 3: Commit final si hay ajustes**

```bash
git add -p  # solo los cambios pendientes
git commit -m "fix: explore polish from smoke test"
```
